<%= render 'shared/errors', obj: @user %>
<h1 style = "text-align: center">会員の新規登録!</h1>

<%= form_for @user, url: :user do |form| %>
	<%= render "form", form: form %>
	<div class="button_area">
		<%= form.submit '作成', class: 'button'%>
	</div>
<% end %>
